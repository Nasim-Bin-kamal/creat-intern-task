import React from 'react';
import Header from '../../components/Header/Header';

const Feedback = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Feedback;